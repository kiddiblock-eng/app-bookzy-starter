# Bookzy — Module Auth/Setup/Dashboard (Starter)

## Lancer en local
```bash
npm install
npm run dev
```

## Contenu
- Next.js 14 (app dir)
- TailwindCSS + Dark mode (classe `dark` via bouton dans Settings)
- Auth (login/register) — mock
- Setup 2 étapes (profil + pays/langue/objectif)
- Dashboard avec Sidebar/Header, sections:
  - Mes Projets (formulaire de génération d’ebook)
  - Templates
  - Générer un site (Bientôt)
  - Historique, Paramètres, Support
- API /api/generate (mock)

## À faire ensuite
- Brancher OpenAI (texte), DALL·E (images), Stripe (paiement), stockage (S3/Supabase)
